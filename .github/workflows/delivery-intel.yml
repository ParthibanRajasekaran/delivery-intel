# ============================================================================
# Delivery Intel — Example GitHub Action
# ============================================================================
# Runs delivery-intel on every push to main and every PR.
# Fails the job if the overall score drops below 40 or critical vulns exist.
#
# Copy this file into your repo at:
#   .github/workflows/delivery-intel.yml
#
# Token resolution (in order):
#   1. DELIVERY_INTEL_TOKEN secret  — custom PAT (optional)
#   2. Built-in GITHUB_TOKEN        — auto-provided by Actions
# ============================================================================

name: Delivery Intelligence

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    # Weekly report every Monday at 8 AM UTC
    - cron: "0 8 * * 1"

jobs:
  analyze:
    name: DORA & Vulnerability Analysis
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci --legacy-peer-deps

      - name: Build CLI
        run: npm run build:cli

      - name: Run Delivery Intel
        env:
          GITHUB_TOKEN: ${{ secrets.DELIVERY_INTEL_TOKEN || secrets.GITHUB_TOKEN }}
        run: |
          node bin/delivery-intel.js ${{ github.repository }} \
            --json \
            --output delivery-intel-report.json

      - name: Upload Report Artifact
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: delivery-intel-report
          path: delivery-intel-report.json
          retention-days: 90

      - name: Check Score Threshold
        run: |
          SCORE=$(jq '.overallScore' delivery-intel-report.json)
          echo "📊 Delivery Intel Score: $SCORE / 100"

          CRITICAL=$(jq '[.vulnerabilities[] | select(.severity == "critical")] | length' delivery-intel-report.json)
          echo "🔴 Critical vulnerabilities: $CRITICAL"

          if [ "$CRITICAL" -gt 0 ]; then
            echo "::error::$CRITICAL critical vulnerability(ies) found"
            exit 1
          fi

          if [ "$(echo "$SCORE < 40" | bc)" -eq 1 ]; then
            echo "::error::Score $SCORE is below the minimum threshold of 40"
            exit 1
          fi

          echo "✅ All checks passed"

      - name: Comment on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v8
        with:
          script: |
            const fs = require('fs');
            const report = JSON.parse(fs.readFileSync('delivery-intel-report.json', 'utf8'));
            const { overallScore: score, doraMetrics, vulnerabilities } = report;

            const ratingEmoji = { Elite: '🟢', High: '🔵', Medium: '🟡', Low: '🔴', 'N/A': '⚪' };

            const body = `## 📊 Delivery Intelligence Report

            | Metric | Value | Rating |
            |--------|-------|--------|
            | **Overall Score** | ${score} / 100 | ${score >= 75 ? '🟢' : score >= 50 ? '🟡' : '🔴'} |
            | Deployment Frequency | ${doraMetrics.deploymentFrequency.deploymentsPerWeek.toFixed(1)}/week | ${ratingEmoji[doraMetrics.deploymentFrequency.rating]} ${doraMetrics.deploymentFrequency.rating} |
            | Lead Time for Changes | ${doraMetrics.leadTimeForChanges.medianHours?.toFixed(1) ?? 'N/A'}h | ${ratingEmoji[doraMetrics.leadTimeForChanges.rating]} ${doraMetrics.leadTimeForChanges.rating} |
            | Change Failure Rate | ${doraMetrics.changeFailureRate.percentage.toFixed(1)}% | ${ratingEmoji[doraMetrics.changeFailureRate.rating]} ${doraMetrics.changeFailureRate.rating} |
            | Vulnerabilities | ${vulnerabilities.length} found | ${vulnerabilities.length === 0 ? '🟢' : '🔴'} |

            <details><summary>Full JSON Report</summary>

            \`\`\`json
            ${JSON.stringify(report, null, 2).slice(0, 60000)}
            \`\`\`

            </details>

            *Generated by [delivery-intel](https://github.com/ParthibanRajasekaran/delivery-intel)*`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body
            });
